# Lightweight test deps for CI (no full Home Assistant install).
# Entity and config_flow tests are skipped when HA is mocked.
# For full tests locally: pip install -r requirements-test.txt
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=4.0.0
aiohttp>=3.14.3
voluptuous>=0.16.0
